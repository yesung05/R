2+3
(3+6)*8
2^3         # 2의 세제곱

# 파일 실행에서는 출력이 없으면 결과가 표시될 수 없음
source("Ch02/2-2.R")  
print(10 %% 3)

# 상수
NULL
NA
NaN
Inf
-Inf

na

log(10)+5      # 로그함수
sqrt(25)       # 제곱근
max(5,3,2)     # 가장 큰 값
a <- 10
b <- 20           
c <- a+b
print(c) 

a <- 125 
a          
print(a)

a <- 10     # a에 숫자 저장
b <- 20
a+b         # a+b의 결과 출력
a <- "A"    # a에 문자 저장
a+b         # a+b의 결과 출력. 에러 발생

x <- c(1,2,3)                   # 숫자형 벡터
y <- c("a","b","c")             # 문자형 벡터
z <- c(TRUE,TRUE, FALSE, TRUE)  # 논리형 벡터
x                               # x에 저장된 값을 출력
y
z

w <- c(1,2,3, "a","b","c")
w

v1 <- 50:90 
v1
v2 <- c(1,2,5, 50:90)  
v2

